#include<cmath>
#include<iostream>
using namespace std;
int main(){
	int x, y;
	cin >> x >> y;
	cout << max(x, y) << endl;
	cout << min(x, y) << " ";
	cout << sqrt(10) << endl;
	cout << sqrt(169);
	for(int i = 1;i<=10;i++){
		cout << rand() << endl;
	}
	return 0;
}
