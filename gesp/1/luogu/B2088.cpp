#include<iostream>
#include<cstdio>
using namespace std;
int main(){
	double a, b, c, d, e, f, g, h, j, k;
	cin >> a >>  b >> c >> d >> e >> f >> g >> h >> j >> k;
	printf("%.1f", a * 28.9 + b * 32.7 + c * 45.6 + d * 78 + e * 35 + f * 86.2 + g * 27.8 + h * 43 + j * 56 + k * 65);
	return 0;
}
